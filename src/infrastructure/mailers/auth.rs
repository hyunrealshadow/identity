//! Auth mailer.
//!
//! Previously used the Loco-scaffolded user model. That model has been
//! removed. This mailer is now a stub; email functionality will be
//! re-implemented once the domain-level mailer abstraction is in place.

#[allow(dead_code)]
pub struct AuthMailer;
