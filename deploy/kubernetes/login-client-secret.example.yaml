apiVersion: v1
kind: Secret
metadata:
  name: identity-login-client
type: Opaque
stringData:
  # Copy these from the one-time installation result; never commit real values.
  client-id: replace-with-installation-client-id
  client-secret: replace-with-installation-client-secret
